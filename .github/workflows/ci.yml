name: MoonBit quality checks
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install independent Tcl oracle
        run: sudo apt-get update && sudo apt-get install -y python3-tk
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install MoonBit
        run: |
          curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash
          echo "$HOME/.moon/bin" >> "$GITHUB_PATH"
      - run: moon version --all
      - run: moon fmt && moon info && git diff --exit-code
      - run: /usr/bin/python3 tools/generate_list_oracle.py && moon fmt && git diff --exit-code
      - run: moon check
      - run: moon test --target wasm-gc
      - run: moon test --target js
      - run: moon build --target js
      - name: Refresh actual browser engine
        run: |
          engine=$(find _build/js/debug/build/cmd/web -type f -name '*.js' | head -n 1)
          test -n "$engine"
          cp "$engine" web/engine.mjs
      - run: node tools/test-demo.mjs
      - run: node tools/test-cli.mjs
      - run: node tools/test-semantic-oracle.mjs
      - run: node tools/test-semantic-oracle.mjs cache
      - run: node tools/test-semantic-oracle.mjs namespace
      - run: node tools/test-semantic-oracle.mjs completion
      - run: node tools/test-semantic-oracle.mjs syntax
      - run: node tools/test-syntax.mjs
      - run: node tools/test-completions.mjs
      - run: node tools/test-namespaces.mjs
      - run: node tools/test-sessions.mjs
      - run: python3 tools/test-http.py
      - run: node tools/semantic-stress.mjs
      - run: node tools/robustness.mjs
      - run: node tools/benchmark.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: quality-evidence
          path: evidence/
