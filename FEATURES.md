# Tcl 兼容矩阵

| 能力 | 当前状态 | 证据/剩余缺口 |
|---|---|---|
| 基础脚本分组/替换与整数运算 | 部分 | 已有独立算术用例；完整 expr、32 位溢出和大整数仍有差距 |
| proc 局部作用域/递归/默认/args | 已实现常见行为 | 系统 Tcl 对照；命名空间、upvar/global 仍未实现 |
| 列表解析与规范构造 | 已实现主要规则 | 180 组规范表示 + 12 组解析场景；Unicode 全面一致性仍待验证 |
| 10 个常用列表命令 | 已实现 | 嵌套 lindex、end、空白保留、空数据和错误对照；lsort/lsearch/lset/linsert/lreplace 尚缺 |
| for/foreach/while/break/continue | 已实现 | 多列表、补空、循环控制和预算对照 |
| catch/error/return | 基础形式 | 返回码 0–4 与基本 resultVar；高级选项、optionsDict 未完成 |
| if 完整语法 | 未完成 | elseif/then 尚缺 |
| 命名空间/数组/dict/字符串/I/O | 未完成 | 保留为追平要求，不能从现有绿色测试推断完成 |
| 全平台、代表性性能与全量上游测试 | 未完成 | 本地两个后端与样例基准不是全部证明 |

目标继续保持 20 项追平；本轮不会因功能子集通过测试而标记全目标完成。
