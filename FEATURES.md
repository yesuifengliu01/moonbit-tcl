# 功能与兼容性边界

## 新增能力

增加固定参数 proc、局部变量、共享输出、递归与 return。

## 尚未达到上游的部分

无默认/可变参数、命名空间、列表命令、数组变量或外部 I/O；整数为 32 位。已有基础能力参见 README 与生成的 `pkg.generated.mbti`。

## 工程交付范围

独立 Git 仓库、独立构建目录、可执行文档、Wasm-GC/JS 测试、真实编译的浏览器与 CLI、边界输入检查、样例基准、CI 配置均随仓库交付。运行记录见 evidence；配置 CI 不代表远端 CI 已运行。没有公开发布或比赛验收结论。
