# 后续追平顺序

1. 补齐运行中命名空间的延迟销毁、自动加载库、全量异常错误码/诊断堆栈、增量脚本解析、完整 string 子命令和原始字节/补充平面边界（基础 Unicode 分类/简单映射已完成）；先保留独立 Tcl 反例，再修改实现。
2. 为真正应用提供 source/文件/通道/包加载及受控宿主接口，建立实际项目的集成验证。
3. 已有有界解析缓存和标量集合延迟表示；继续补字节码、变量槽位及嵌套/数组对象路径。0.5.0 历史七组复用会话负载仍比 Tcl 慢约 4.0–6.9 倍，需持续用独立热路径对照追踪。
4. 扩展上游测试、跨系统验证和长时间负载；建立持久会话累计内存配额与回收机制。

全部工作保持本地独立仓库，不上传、不发布、不提交比赛。已通过的功能子集不等于完整追平。
