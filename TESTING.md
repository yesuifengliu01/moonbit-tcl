# Validation contract

Run verify.ps1 with MoonBit and Node 24 on PATH (or pass -MoonPath). It formats, generates the public API, checks without warnings, tests WasmGC and JS, rebuilds the shipped engine, then runs demo, CLI, semantic differential, session, malformed-input and benchmark checks. Expected counts and current results are recorded in evidence/regexp-upgrade.json.

The 726 original semantic fixtures are in tools/semantic-cases.py. tools/generate-semantic-oracle.py creates a fresh system Tcl 8.6.15 interpreter per program. Only puts is adapted to collect stdout. Expected results are written to evidence/semantic-reference.json and semantic_oracle_test.mbt. Successful programs compare exact result and output strings; expected errors compare rejection, not wording. The separate legacy generator retains its 248 list/procedure/control cases. Ordinary verification consumes checked-in expected values; regeneration requires the pinned reference version.

New session checks run the real exported module, JSON-lines CLI child processes, and the actual web Worker module through a Node message adapter. Browser acceptance is a separate live CUA run: example, continued state, output before errors, recovery, reset, cancellation, timeout, import/download and mobile layout. Node Worker checks do not stand in for browser acceptance.

The malformed-input corpus remains 307 seeded bounded inputs. Resource experiments and performance measurements are separate evidence; neither is proof of arbitrary hostile-input isolation. The persistent interpreter has no global memory quota.

After the final changes: run the full verifier; regenerate/format/info/build twice and compare source/artifact hashes; bind the evidence manifest to Git blob SHA256s. Reports from older dates are historical. No remote CI, upload, external service, or publication is performed.

For the current optimization regressions, run python tools/generate-semantic-oracle.py cache. This generates 340 independent stateful and arithmetic fixtures in cache_oracle_test.mbt plus evidence/cache-reference.json. Four hand-authored public API tests cover cache clearing, eviction, unchanged execution budgeting and atomic failure of lazy collection mutation. Both oracle groups run through the compiled JS bridge and both MoonBit targets.

To reproduce performance: extract web/engine.mjs from local commit 8c161948071003589cb203f1032e152ee26a1810 into a temporary baseline-engine.mjs using a binary-safe Git subprocess; run python tools/benchmark-semantic.py, then node tools/compare-performance.mjs ABSOLUTE_BASELINE_PATH. The comparison tool rejects a baseline whose SHA256 differs from that Git blob. It alternates engine order, excludes 20 warmups and measures 30 samples for each of seven workloads, checking all results against the independent Tcl output. Timing is evidence, never a CI pass/fail threshold. CPU profile summaries identify allocation and repeated parsing costs, not language coverage.

The semantic-upgrade.json manifest describes the earlier 0.4.0 commit. The 0.5.0 proof checker used performance-upgrade.json; do not apply the older source hashes to the newer tree.

For 0.6.0, python tools/generate-semantic-oracle.py namespace generates 312 independent namespace/lifecycle fixtures, including 96 deterministic 12-step mutation traces. Five public API regressions cover persistent caches/import identity, rejected import cycles, bounded ensemble/unknown recursion, the 10000-command table limit and atomic configuration failure. tools/test-namespaces.mjs adds 20 persistent module/CLI/actual Worker assertions. Successful oracle programs compare exact values/output; error cases compare rejection only. Import query order is unspecified; multi-name queries used for comparisons must be sorted. Built-in automatic library loading, active-namespace deferred destruction, root deletion and exhaustive upstream coverage remain incomplete.

The 0.6.0 checker selected namespace-upgrade.json. Earlier performance-upgrade.json and semantic-upgrade.json manifests, browser reports and performance-comparison.json describe their respective historical engine hashes and are not evidence for the current engine. Namespace browser checks and current benchmark/generation reports are recorded separately. No remote CI or cross-platform run is implied.

Run python tools/check-generation.py ABSOLUTE_MOON_PATH for the reproducible two-cycle gate. For the current performance comparison, extract the 0.5.0 engine at 6bd1de88b98b86ed315ad0e9c38695688e7f9449, regenerate the system Tcl benchmark, and pass that commit and namespace-performance.json as the third and fourth arguments to tools/compare-performance.mjs. The baseline is verified against Git before import. It is a local timing comparison, not a maturity pass threshold.

For 0.7.0, python tools/generate-semantic-oracle.py completion generates 321 independent completion fixtures. The return matrix contains 105 code/level/procedure-depth combinations; handler/finally matrices add 72. Normalized matrices remove generated -errorinfo/-errorline/-errorstack, replace error message bodies with a placeholder, recursively normalize -during, and sort dictionary keys. This verifies completion codes, levels, custom options and side effects, not byte-for-byte generated diagnostics. Ten explicit metadata/location programs and 29 native error-message/error-code programs compare selected fields directly. Other cases exercise cleanup, rethrow, variable binding failure, Tcl lexical comparisons and rejection of malformed options. Only the documented subset of native error codes is implemented; generated errorstack uses portable INNER/CALL command frames and is not Tcl bytecode-identical.

Seven new public API regressions cover eval_catch raw levels, snapshots, cleanup chains, cached line locations, budget exhaustion, output/API misuse and portable stack stability. tools/test-completions.mjs runs persistent module sessions, the real JSONL CLI and actual web Worker with a Node message adapter. The live browser report remains separate. eval_catch does not consume a return level, unlike eval. Successful calls clear transient return options; diagnostic globals retain the last error, as in Tcl.

The 0.7.0 proof checker selected completion-upgrade.json; earlier milestone manifests refer to their own historical trees. The generation gate now includes all four semantic suites and writes generation-completion.json. For the current performance comparison, extract the 0.6.0 engine at 684380f5da86b9d2ea06f2597920979075015431 and pass that commit plus completion-performance.json to tools/compare-performance.mjs after regenerating the Tcl benchmark. Fixed timings are not CI thresholds. Remote CI has not run.

The completion host suite also checks isolated UTF-16 surrogate errors through the legacy text and structured JSON APIs. Script error rendering uses JSON string serialization instead of the Debug formatter, preventing a host panic while preserving code units.

For 0.8.0, python tools/generate-semantic-oracle.py syntax generates 1019 independent programs: eight substitution-flag combinations, exceptional bracket completion versus malformed tails, array-variable parsing boundaries, cross-kind substitutions, option abbreviations, explicit and 400 deterministic generated completeness queries, grouped-word continuation and comment backslash parity. Substitution fixtures compare completion status, normalized error results, side effects and selected options. Completeness results and ordinary successful values/stdout compare exactly. Errors are not a full diagnostic-text equivalence claim.

Six public API regressions exercise the new is_complete API and persistent evaluator behavior, including no execution while checking syntax, cached substitution side effects, whole-variable exceptional returns, shared command budgets, output/nesting limits and multiline fragments. tools/test-syntax.mjs runs persistent module sessions, real JSONL CLI processes and the actual Worker with a Node adapter. Live UI acceptance remains separate. info patchlevel identifies this implementation's 0.8.0 version, not a claim to be a native Tcl release.

The 0.8.0 checker selected syntax-upgrade.json, and the generation gate includes five suites plus the legacy list generator. Use the same seven-workload timing method with the baseline engine from 252f99deb79402fa3aa62ef3734cc2e938d6c555 and output syntax-performance.json. Earlier milestone manifests and reports refer to their recorded trees/engines; no remote CI or cross-platform execution is implied.

For 0.9.0, generate-unicode.py verifies the pinned official UnicodeData 16 SHA256 before deriving BMP category intervals and simple case mappings. The imported data and license are under vendor/ucd-16.0.0; no native Tcl lookup tables were copied. audit-unicode.py checks every BMP codepoint against native Tcl 8.6.15, including the public case-conversion byte-width restriction. Its 1048576 property/conversion checks are a data audit, not compiled command coverage.

The 1866 unicode fixtures include 107 batches at generated category boundaries, 75 batches covering all 2369 simple-case entries and exact-nocase search, 840 classification/strict/failure combinations, 284 numeric prefix/range cases, and list failures/options/trim/glob/escapes/UTF-16 operations. Inputs are original, expected results come from a fresh native interpreter per fixture. The boundary selection depends on the source table; expected values do not. Both MoonBit backends and the shipped JS bridge execute these fixtures. Success compares complete result/stdout strings; expected errors compare rejection only.

Five hand-authored API regressions cover isolated-unit roundtrips, local supplementary case preservation, aliased failindex mutation, cached dynamic case input and command/output limits. tools/test-unicode.mjs adds 30 checks across module sessions, the real JSONL CLI and actual Worker, including JSON transport of isolated surrogates. The separate live browser report has 13 input/run/reset and result/output/error checks; it does not rerun import/download, cancellation/timeout or mobile layout.

Reference exclusions are explicit: tools/probe-unicode-reference.py runs risky native inputs in separate subprocesses with timeouts. Three supplementary case conversions exit with Windows access violation 0xC0000005; two trim inputs return truncated UTF-8 exposed by tkinter as surrogateescape bytes. See unicode-reference-limits.json. These five probes are not part of the passing oracle total. Supplementary case conversion in unicode_test.mbt is a local UTF-16 preservation guarantee. Raw Tcl byte-string/object representation parity remains incomplete. Tcl 8.6 category predicates use the low 16 bits of decoded supplementary codepoints, except ascii/xdigit; tests intentionally preserve this reference behavior rather than modern full-plane Unicode semantics.

The 0.9.0 proof checker selected unicode-upgrade.json. check-generation.py now regenerates Unicode tables, all six semantic suites and legacy fixtures, followed by fmt/info/build and shipped-engine refresh, twice. It writes generation-unicode.json. Performance uses the 0.8.0 baseline d3103d8fbc35f6057c5d1b755056163a8389c7ec and unicode-performance.json. Historical reports retain their original hashes; no full upstream, remote CI or cross-platform pass is implied.

For 0.10.0, python tools/generate-semantic-oracle.py conversion evaluates 4970 original programs on native Windows Tcl 8.6.15. Exactly 4968 match and two retained differences document raw UTF-8 fragments and double-object identity. All are preserved in conversion-reference.json/comparison.json. The generator pins each known reference result; compiled Known difference tests validate the separately named local contract. The JS comparison fails on any changed local contract, unexplained mismatch or silently resolved annotation. Known differences are never added to the matched total. Valid CESU-8 surrogate triples returned by tkinter are normalized to individual UTF-16 units; other invalid bytes remain visible. Success compares result/stdout; error cases compare rejection only.

The new cases include 91 batches with all 2098 positive finite powers of two, 300 negative powers, and 500 seeded random finite binary64 inputs. Separate format matrices exercise exact binary rounding, flags, sizing, overflow and subnormals. Six public API tests check exact ties, integer width distinctions, validation/write ordering, alias/cache behavior, resource limits and isolated surrogate transport. These are not exhaustive float, object, Unicode or upstream suites. Default integer sizing is explicitly Windows word32/wide64, not Linux-native sizing. Scalar numeric-object identity and raw byte representation remain incomplete.

The 0.10.0 proof checker selected conversion-upgrade.json. check-generation.py regenerates all seven suites, legacy fixtures and Unicode tables, then fmt/info/build and the shipped engine twice, writing generation-conversion.json. tools/test-conversion.mjs checks persistent module, actual JSONL CLI and the actual Worker with a Node adapter. Browser evidence is separate and only claims the operations recorded there. Compare existing workloads to baseline 8b70b4d8ac164125b78d417b548c85b5350b8a83 with conversion-performance.json; tools/benchmark-conversion.py measures three new format/scan workloads against native Tcl, without a previous-version claim. No full application, remote CI or cross-platform pass is implied.

For 0.10.1, the optimization preserves public semantics and reuses the full 9851-test suite on JS and WasmGC, plus all existing differential and host checks. No new behavioral coverage is claimed. The native conversion matrix remains 4968 matched programs, two explicit known differences and zero unexplained differences. The separate browser report records eight numeric/precision/error/recovery/reset checks; other UI workflows are historical.

To reproduce the performance campaign, extract web/engine.mjs from commit 8327af4f9da3c8d610f79452530c18f630da5b9e with a binary-safe Git subprocess. Run python tools/benchmark-conversion.py number-dispatch-workloads.json and python tools/benchmark-semantic.py, then python tools/repeat-performance.py ABSOLUTE_BASELINE_PATH 8327af4f9da3c8d610f79452530c18f630da5b9e number-dispatch-conversion-performance.json number-dispatch-workloads.json and repeat with number-dispatch-general-performance.json semantic-benchmark-reference.json. The runner executes exactly five fresh Node processes, each with 20 warmups and 30 interleaved samples per workload; it preserves all five reports and summarizes run-ratio median/min/max. Both engines' results and stdout are checked, baseline bytes must match Git, and reference bytes are hashed. There is no timing pass/fail threshold or significance test.

node tools/profile-conversion.mjs OUTPUT.json records V8 self-time summaries for the three conversion workloads. Baseline and current profile summaries are bound to their respective engine hashes; they are diagnostics, not benchmark timings. The unchanged baseline source programs are read from conversion-workloads.json. Current conversion performance is about 10x faster for receipt and high-precision formatting, while seven general run-ratio medians range 0.984-1.045. The bigint row is about 4.5% slower at the median, with all five ratios retained (0.987-1.080); this is not a proof of no regression or full performance parity.

At 0.10.1, the checker selected number-dispatch-upgrade.json and the two-cycle generator wrote generation-number-dispatch.json. Previous manifests and reports describe historical versions. Existing source/generation guards still apply; the old conversion-workloads.json is preserved and fresh native/Node measurements use number-dispatch-workloads.json.


For 0.11.0, generate-semantic-oracle.py value runs 855 original programs against a fresh native Tcl 8.6.15 interpreter for each case. The matrix covers seven numeric inputs through 91 transport paths, distinct same-text values, shared type conversions, 120 seeded arithmetic inputs, typed script/prefix forwarding, custom completion options and nested copy isolation. Values that are rejected by native Tcl compare rejection only; successful results and stdout compare exactly. All 855 currently match. The existing 4970 conversion programs now have 4969 matches and one retained raw-UTF-8 difference: the resolved scan-double-object-identity annotation was removed, and its program now requires the native result. One local known-difference contract remains in the 10712 backend tests.

Six new public-API tests cover preservation across separate eval calls/cache clearing/errors, distinct values with identical display text, nested copy isolation, custom completion options, shared representation conversion and typed-script limits. tools/test-values.mjs adds 30 assertions through persistent module sessions, a real JSONL CLI child process and the actual Worker module with a Node message adapter. The live browser report records ten precision/transport/copy/error/recovery/reset checks and no console warnings/errors; import/download, cancellation/timeout and mobile layout were not repeated. examples/values.tcl is independently evaluated by native Tcl and the real file CLI. Public callable signatures remain unchanged; pkg.generated.mbti adds the opaque TclValue and ValuePayload type names.

values.mbt follows Tcl's separation of a textual and an internal representation, with separate number/list/dictionary payloads; retrieving text does not parse it back into a number. Internal type conversion can replace a shared representation, while collection edits build new containers. Scalar loop accumulators retain the existing lazy collection encoding path; reading such a cell produces a value snapshot before subsequent writes. List-script execution retains script-size, argument-count, recursion and evaluation-budget limits. This is not the complete Tcl_Obj ABI, reference-count model, bytecode compiler, string-interning behavior, byte storage or whole-session memory management. Tcl documentation: https://www.tcl-lang.org/man/tcl8.6/TclLib/Object.htm . Behavioral expected values are pinned to native 8.6.15, independently of the currently published manual version.

At 0.11.0, the proof checker selected value-upgrade.json; check-generation.py regenerated all eight suites, the legacy fixtures, Unicode tables, fmt/info/build and shipped engine twice, recording generation-values.json. The baseline for both fixed five-process timing campaigns is 18552dfaeb1e902d97afc02f29a19d3df24697ed. Run benchmark-semantic.py, benchmark-conversion.py value-workloads.json, then repeat-performance.py with that baseline/commit and value-general-performance.json or value-conversion-performance.json (respective reference files semantic-benchmark-reference.json and value-workloads.json). All runs are retained and engine/reference hashes checked. The general ratio medians range 0.704–1.128; receipt/scan/high-precision medians are 0.904/1.160/1.197. Regressions remain explicit; there is no significance, full application or cross-platform performance claim.


For 0.11.1, dispatch/control read value text directly instead of copying all parameters. Procedure calls create text arrays only when constructing error context. Native command words are replaced only in a copied array when normalization is needed. Cached script execution invokes a single-command helper directly, and empty results are allocated only when needed. This refactor introduces no new public behavior or signatures; it reuses the 10712 backend tests and all eight independent matrices, including budget/recursion/argument/result limits, error context, aliases, typed script forwarding, empty substitution ordering and shared representation conversions. Eleven real browser checks additionally cover canonicalization without mutation, error recovery and reset. The browser suite did not repeat import/download, cancellation/timeout or mobile layout.

At 0.11.1, the proof checker selected dispatch-upgrade.json. The two-cycle generator records generation-dispatch.json. The complete verifier log is verify-dispatch.log. Current workload references are semantic-benchmark-reference.json and dispatch-workloads.json, regenerated against native Tcl 8.6.15. Extract the committed engine with binary-safe git show and run repeat-performance.py BASELINE COMMIT OUTPUT REFERENCE. The 0.11.0 baseline is 25f500621277908ab583419a3c21ca0fcd32dc16, producing dispatch-general-performance.json and dispatch-conversion-performance.json. A second direct comparison to pre-value 0.10.1 (18552dfaeb1e902d97afc02f29a19d3df24697ed) produces dispatch-prevalue-general-performance.json and dispatch-prevalue-conversion-performance.json. All four campaigns use exactly five fresh sequential processes with 20 warmups and 30 interleaved measurements. Every result/stdout is checked and all rounds retained. The exploratory dispatch-pilot-performance.json predates the final control-flow cleanup and is not used for current timing claims. It is preserved as a diagnostic record, with its distinct engine hash.

node tools/profile-runtime.mjs OUTPUT.json samples ten fixed warm-session workloads (20 warmups, 1000 iterations, 500 microsecond requested interval). Both value-runtime-profile-baseline.json and value-runtime-profile-current.json retain top 40 self-time samples per workload. Native outputs are checked; inspector overhead and sampling variability prevent treating these as allocation counts or unbiased benchmarks. Baseline profile reference digests describe files at commit 25f5006; the current semantic timing reference was regenerated, but all ten source/result pairs are unchanged. Current five-process timing medians vs 0.11.0 range 0.851–0.968; array and receipt min/max ranges cross 1. Against 0.10.1, array/scan/exact medians remain 1.025/1.024/1.011. No significance, full application, peak-memory or cross-platform claim is made.


For 0.12.0, run python tools/generate-semantic-oracle.py switch to probe all 5863 original programs in isolated native interpreters. The persistent worker has a two-second deadline per case and is killed/restarted on timeout. The current report has 5828 completed cases (5650 exact success results/stdout and 178 expected rejections) plus 35 native timeouts, never counted as passing comparisons. Structured messages/codes wrapped in catch are ordinary exact-result successes. Full raw error text/stack parity is not claimed. Native timeout failures remain in switch-reference-limits.json with their complete inputs; this report is independently regenerated in both stability cycles.

switch_oracle_test.mbt groups forty independently labeled assertions per test (146 tests), because one generated test per program would exceed the compiler's test-driver text segment limit. All 5828 programs still run separately with a fresh interpreter. Seven additional public API regressions check state/cache persistence, skipped-pattern compilation, typed bodies, variable write ordering/failure, completion and namespace scope, representation changes and resource failure/recovery. Full verify has 10865 tests per backend, including the one preexisting raw-byte known-difference local contract; the test count is not a compatibility percentage. test-switch.mjs adds 34 module, actual JSONL CLI and real Worker-module checks. Thirteen real browser actions verified the example, persistence, glob fallthrough, captures/indices, no-match/default semantics, nested capture priority, backreferences/lookahead, errors, recovery and reset. Browser console logs, file import/download, cancellation/timeout and mobile layout were not repeated. The page and local service were closed.

The parser and state evaluator are original MoonBit code and do not call host regex engines. Semantics were checked against https://www.tcl-lang.org/man/tcl8.6/TclCmd/switch.htm, https://www.tcl-lang.org/man/tcl8.6/TclCmd/re_syntax.htm and the quantifier decomposition in https://github.com/tcltk/tcl/blob/core-8-6-15/generic/regcomp.c. These web manuals can describe a newer patch release; test expectations are pinned to the installed Windows Tcl 8.6.15. The local implementation enumerates candidate states and applies explicit pattern/capture/depth/state/instruction limits; no linear-time, complete ARE, peak-memory or native resource-limit parity is claimed. Valid complex native patterns can exceed the local limits.

The current manifest is switch-upgrade.json; check-proof.py checks it against staged or committed blobs. check-generation.py regenerates all nine matrices, the native limit report, other generated fixtures, fmt/info and the shipped engine twice, recording generation-switch.json. Public API signatures remain unchanged. The complete verifier log is verify-switch.log. benchmark-switch.py times five new warm-session workloads independently against native Tcl (10 warmups/30 measurements). repeat-performance.py compares the exact engine from commit 247c318e6961ea38bd6e56e7fff847f5274ec241 against the current engine in two fixed five-process campaigns, each with 20 warmups/30 interleaved measurements. All ten source/result pairs are unchanged. switch-general-performance.json and switch-conversion-performance.json retain every round: short dictionary median ratio 1.205, other workloads 0.964–1.071. New switch/native ratios are about 1.7–15.9. Timing is one-host, warm-session evidence with different host adapters, no significance test or full application/cross-platform acceptance. Performance parity remains open.

Quantifier prefixes share AST nodes instead of recursively cloning uncaptured subtrees. The seventh public regression builds 48 nested quantifiers below the parser limit, verifies bounded execution failure and subsequent session recovery. The three switch-before-sharing-*.json reports retain the earlier development engine timings (different hash), including its 1.308 short-dictionary median ratio; final claims use only the rebuilt shared-node engine.

For 0.13.0, the regexp suite contains 6299 independently evaluated Tcl 8.6.15 programs: 6287 exact success result/stdout matches, four expected rejections, and eight explicit local-contract differences for native bytecode-only nocase abbreviations. Those differences are not compatibility passes. The generator validates their pinned reference results and groups the cases in 158 backend tests. All eight new public API regressions run on JS and WasmGC; both backends pass 11031 tests. The existing conversion suite retains its single raw UTF-8 fragment difference, and switch retains 35 excluded native timeouts.

Run python tools/generate-semantic-oracle.py regexp, node tools/test-semantic-oracle.mjs regexp and node tools/test-regexp.mjs. The native worker has a two-second deadline per case; the new suite has zero exclusions. The 48 host checks exercise persistent module sessions, real JSONL CLI and actual Worker module; live browser evidence is separate. Public tests include instruction budgets, output caps, atomic failed replacement, ordered capture writes, numeric representations and deep shared metadata analysis.

Metadata is derived from lexical notes and a finite relation over neighboring character categories, with shared node memoization. Backreferences use the captured expression's regular approximation; lookaheads remain opaque for these compile properties. Character sets are analyzed using Unicode intervals. Neither exhaustive regular-expression compilation equivalence nor aggregate memory bounds are claimed.

Current timing baseline is local commit c302c665d2af526f9be023116cbb540196228707 (0.12.0). Two fixed five-process campaigns retain every round in regexp-general-performance.json and regexp-conversion-performance.json. Six new native/Node workloads are in regexp-workloads.json. Four earlier development files regexp-before-interval-*.json retain their distinct engine hash. No performance acceptance threshold or cross-platform conclusion is implied.

The generation gate now includes ten semantic suites, both regex exclusion reports, Unicode/list regeneration and two fmt/info/build/engine-copy cycles, recorded in generation-regexp.json. check-proof.py selects regexp-upgrade.json first. Old milestone manifests describe their own committed trees. New public API signatures remain unchanged; no remote publishing is performed.

Four additional bounded native edge probes in tools/probe-regexp-start.py cover global empty matches at the largest signed starting index. They generate regexp-start-reference.json and are asserted within the existing start-index API regression on both targets; they are not included in the 6299-case matrix. Termination is checked before advancing an out-of-range index, avoiding signed wraparound. Both generation cycles rerun these probes.
