proc summarize {values {scale 2}} {
 set out {}
 foreach value $values {
  if {$value < 0} {continue}
  lappend out [expr {$value * $scale}]
 }
 return $out
}
set result [summarize {1 -2 3 5} 3]
puts [join $result {, }]
list $result [llength $result]