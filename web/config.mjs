export default {"slug": "tcl", "title": "Tcl 脚本解释器", "scope": "过程、默认/可变参数、列表、for/foreach/while、break/continue、catch/error", "source": "https://www.tcl-lang.org/man/tcl8.6/TclCmd/Tcl.htm", "limitations": "仍缺命名空间、数组变量、完整表达式/字符串/dict 命令、I/O，以及 return/catch 的完整选项；不是完整 Tcl 8.6", "recommendation": "保留候选：先补边界和上游兼容范围，再决定是否申报。", "version": "0.3.0", "example": "proc summarize {values {scale 2}} {\n set out {}\n foreach value $values {\n  if {$value < 0} {continue}\n  lappend out [expr {$value * $scale}]\n }\n return $out\n}\nset result [summarize {1 -2 3 5} 3]\nputs [join $result {, }]\nlist $result [llength $result]"};
