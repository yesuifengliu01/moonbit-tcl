export default {"slug": "tcl", "title": "Tcl 工作台", "scope": "提取文本、批量替换；支持 regexp/regsub、switch、异常、命名空间和集合，并保留会话。", "source": "https://www.tcl-lang.org/man/tcl8.6/TclCmd/Tcl.htm", "limitations": "Tcl 8.6 兼容子集：尚缺文件/进程/网络 I/O、包加载、自动加载库及完整编译和诊断行为。复杂正则受执行预算限制；脚本限 10 万字符，单次运行限 5 秒。", "version": "0.17.0", "example": "# Extract records and redact email addresses without losing the session.\nset text {Tea:12 Coffee:24 Tea:8}\nset totals {}\nforeach {whole name amount} [regexp -all -inline {([[:alpha:]]+):([0-9]+)} $text] {\n    dict incr totals [string tolower $name] $amount\n}\nputs $totals\nset message {Contact alice@example.com or bob@example.org}\nset count [regsub -all {([[:alnum:]_.]+)@([[:alnum:].]+)} $message {<hidden>@\\2} redacted]\nputs $redacted\nregexp -indices {Coffee:([0-9]+)} $text record amount\nlist $count [dict get $totals tea] $record $amount\n"};
