export default {"slug": "tcl", "title": "Tcl 工作台", "scope": "运行脚本、保留会话；支持命名空间、变量别名、数组、字典和大整数表达式。", "source": "https://www.tcl-lang.org/man/tcl8.6/TclCmd/Tcl.htm", "limitations": "Tcl 8.6 兼容子集：尚缺文件/进程/网络 I/O、regexp、包加载、命名空间导入导出和完整异常选项。脚本限 10 万字符，单次运行限 5 秒；不等同于完整 Tcl。", "version": "0.5.0", "example": "namespace eval ledger {\n variable totals {}\n proc add {name amount} {\n  variable totals\n  dict incr totals $name $amount\n }\n}\nforeach {name amount} {tea 12 coffee 24 tea 8} {\n ledger::add $name $amount\n}\ndict for {name total} $::ledger::totals {\n puts \"$name: $total\"\n}\nexpr {2**80}"};
