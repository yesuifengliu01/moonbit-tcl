export default {"slug": "tcl", "title": "Tcl 脚本解释器", "scope": "分组、变量/命令替换、set/incr/expr/if/while", "source": "https://www.tcl-lang.org/man/tcl8.6/TclCmd/Tcl.htm", "limitations": "整数表达式和 set/incr/puts/expr/if/while；不含 proc、命名空间、数组变量、列表命令、文件/进程/网络 I/O；32 位整数，不是完整 Tcl 8.x。", "recommendation": "保留候选：先补边界和上游兼容范围，再决定是否申报。", "version": "0.1.0", "example": "set total 0\nset i 1\nwhile {$i <= 5} {\n set total [expr {$total + $i}]\n incr i\n}\nputs \"sum=$total\"\nset total"};
